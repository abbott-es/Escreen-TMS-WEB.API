meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/client-api/api/client
  body: json
  auth: inherit
}

body:json {
  {
    "companyName": "string",
    "user": {
      "roleID": "guid",
      "userInfo": {
        "firstName": "string",
        "lastName": "string",
        "email": "string",
        "contactNumber": "string",
        "licenseNumber": "string"
      },
      "auth": {
        "username": "string",
        "password": "string"
      }
    },
    "locations": [
      {
        "name": "string",
        "address": "string",
        "type": "string"
      }
    ]
  }
}

settings {
  encodeUrl: true
}

docs {
  Create one client data
}
