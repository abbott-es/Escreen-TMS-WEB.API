meta {
  name: create
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/role-api/api/role
  body: json
  auth: inherit
}

body:json {
  {
    "roleName": "string"
  }
}

settings {
  encodeUrl: true
}
