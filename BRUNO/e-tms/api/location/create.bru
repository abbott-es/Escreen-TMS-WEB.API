meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/location-api/api/location
  body: json
  auth: inherit
}

body:json {
  {
    "name": "string",
    "address": "string",
    "type": "string",
    "clientID": "guid"
  }
}

settings {
  encodeUrl: true
}
