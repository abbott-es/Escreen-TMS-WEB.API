meta {
  name: get-one
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/location-api/api/location/{{id}}
  body: none
  auth: inherit
}

params:query {
  ~Includes: {{Includes}}
}

vars:pre-request {
  id: guid
  Includes: DataToInclude
}

settings {
  encodeUrl: true
}
