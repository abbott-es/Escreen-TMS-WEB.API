meta {
  name: edit
  type: http
  seq: 5
}

put {
  url: {{BASE_URL}}/vehicle-api/api/vehicle?Includes={{Includes}}
  body: json
  auth: inherit
}

params:query {
  Includes: {{Includes}}
}

body:json {
  {
    "vehicleID": "guid",
    "model": "string",
    "plateNumber": "string",
    "chassis": {
      "chassisID": "guid",
      "vehicleID": "guid",
      "type": "string",
      "serialNumber": "string"
    }
  }
}

vars:pre-request {
  Includes: PropertyToUpdate
}

settings {
  encodeUrl: true
}

docs {
  Edit client data with their new client info with includes pass required parameter to update
}
