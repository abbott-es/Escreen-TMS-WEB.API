meta {
  name: get-one
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/vehicle-api/api/vehicle/{{id}}
  body: none
  auth: inherit
}

params:query {
  ~Includes: {{Includes}}
}

vars:pre-request {
  id: guid
  Includes: dataToInclude
}

settings {
  encodeUrl: true
}

docs {
  Get one vehicle having client id
}
