meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/vehicle-api/api/vehicle
  body: json
  auth: inherit
}

body:json {
  {
    "model": "string",
    "plateNumber": "string",
    "chassis": {
      "type": "string",
      "serialNumber": "string"
    }
  }
}

settings {
  encodeUrl: true
}

docs {
  Create one vehicle data
}
