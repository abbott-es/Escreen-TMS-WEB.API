meta {
  name: refresh-token
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/authentication-api/api/authentication/refresh-token
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "data"
  }
}

settings {
  encodeUrl: true
}
