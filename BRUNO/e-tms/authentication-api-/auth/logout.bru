meta {
  name: logout
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/authentication-api/api/authentication/logout
  body: json
  auth: inherit
}

body:json {
  {
    "accessToken": "token",
    "refreshToken": "token"
  }
}

settings {
  encodeUrl: true
}
