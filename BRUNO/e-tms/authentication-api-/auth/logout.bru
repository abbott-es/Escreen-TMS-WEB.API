meta {
  name: logout
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/authentication-api/api/sso/authentication/logout
  body: json
  auth: none
}

headers {
  Authorization: {{bearerToken}}
  X-Cache-Policy: {{cachePolicy}}
  X-RateLimit-Policy: {{rateLimitPolicy}}
}

body:json {
  {
    "accessToken": "guid",
    "refreshToken0": "guid"
  }
}

settings {
  encodeUrl: true
}
