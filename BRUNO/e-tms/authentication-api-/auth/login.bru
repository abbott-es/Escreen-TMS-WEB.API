meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/authentication-api/api/authentication/login
  body: json
  auth: none
}

body:json {
  {
   "Username": "test",
    "Password": "test"
  }
}

settings {
  encodeUrl: true
}
