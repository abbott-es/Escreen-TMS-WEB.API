meta {
  name: create-session
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/authentication-api/api/sso/session/create-session
  body: json
  auth: inherit
}

headers {
  Authorization: {{bearerToken}}
}

body:json {
  {
    "tokenId": "guid"
  }
}

settings {
  encodeUrl: true
}
