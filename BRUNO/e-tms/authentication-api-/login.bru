meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/authentication-api/api/sso/authentication/login
  body: none
  auth: bearer
}

headers {
  Authorization: {{bearerToken}}
}

settings {
  encodeUrl: true
}
