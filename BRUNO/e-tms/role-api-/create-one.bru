meta {
  name: create-one
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/role-api/api/role
  body: json
  auth: inherit
}

body:json {
  {
    "RoleName" : "testers2"
  }
}

settings {
  encodeUrl: true
}
